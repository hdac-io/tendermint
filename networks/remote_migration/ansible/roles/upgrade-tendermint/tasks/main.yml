---

- name: Copy binary
  copy:
    src: "{{BINARY}}"
    dest: /usr/bin/tendermint
    mode: 0755
  notify: restart tendermint

- name: Copy new genesis.json file, if available
  when: "GENESISFILE is defined and GENESISFILE != ''" 
  copy:
    src: "{{GENESISFILE}}"
    dest: /home/tendermint/.tendermint/config/genesis.json
  notify: restart tendermint

- name: Download genesis.json URL, if available
  when: "GENESISURL is defined and GENESISURL != ''"
  get_url:
    url: "{{GENESISURL}}"
    dest: /home/tendermint/.tendermint/config/genesis.json
    force: yes
  notify: restart tendermint

- name: Reset network
  when: UNSAFE_RESET_ALL | default(false) | bool
  command: "sudo -u tendermint tendermint unsafe-reset-all"
  notify: restart tendermint

